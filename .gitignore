# Python cache files
__pycache__/
*.py[cod]
*$py.class

# Instance folder (contains database, secrets, etc.)
instance/

# Virtual environment (already handled by venv/.gitignore in Py 3.13+, but good to have)
venv/

# Config file (contains secret key, etc.)
config.py

# Project Structure
project_structure.txt